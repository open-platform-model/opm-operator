# Specs — operator-bump-k8s-stable

No capability spec deltas for this change. `proposal.md`'s Capabilities section lists no new or modified capabilities: this is a `k8s.io/*`/`sigs.k8s.io/controller-runtime` dependency-version bump with no controller-observable behavior change (no CRD, reconcile-phase, or API-visible change). See `design.md` for the technical decisions and verification plan that stand in place of a spec delta here.
