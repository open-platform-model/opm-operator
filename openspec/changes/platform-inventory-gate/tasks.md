# Tasks: platform-inventory-gate

Three sections per design.md. Section 1 is the spike (design.md § Section 1 is a spike): it lands the library pin and measures the test platform's inventory before any verdict is written.

## 1. Library pin to v1.0.0-alpha.32 (go.mod)

- [ ] 1.1 `go get github.com/open-platform-model/library@v1.0.0-alpha.32 && go mod tidy`, then `go build ./...`. Verify: `go.mod` names alpha.32 and `platform.ContractInventory` exposes `Comparable` and `Discriminated` (`go doc github.com/open-platform-model/library/opm/platform.ContractInventory`).
- [ ] 1.2 Spike the read: with `buildKernelOrSkip`, generate and build the test platform (`testCatalogPath()` at `fixtures.CatalogVersion()`) and read `Contracts()` off it in a throwaway spec (deleted before the commit); record `len(DefinedBy)`, `Unfulfilled`, `OverSubscribed`, `Comparable`, `Routable` and `Discriminated` in design.md § Context beside the measured state. Verify: the finding is written, and the generated module's core pin reads alpha.10 with no operator constant involved.
- [ ] 1.3 `task dev:fmt dev:vet dev:lint dev:test` green (the live specs run against `CUE_REGISTRY` or skip), then commit `fix(deps): bump library to v1.0.0-alpha.32`.

## 2. The condition and the reasons (internal/status, api/v1alpha1)

- [ ] 2.1 In `internal/status/conditions.go`, add `ContractsFulfilledCondition = "ContractsFulfilled"` and the reasons `OverSubscribedContractsReason`, `ComparablePredicatesReason`, `UnfulfilledContractsReason`, `ContractsFulfilledReason`, `NoContractsDefinedReason`, each with the doc comment style the file uses (what state it reports, and why it is distinct). Verify: `go vet ./internal/status/...` passes and every reason names the enhancement decision it serves.
- [ ] 2.2 Update the `PlatformStatus.Conditions` doc comment in `api/v1alpha1/platform_types.go`: Ready's two new `False` reasons and the non-gating `ContractsFulfilled` condition with its three reasons. Then `task dev:manifests dev:generate` and `task operator:installer`. Verify: `config/crd/bases/opmodel.dev_platforms.yaml` and `dist/install.yaml` carry the new description text and no other diff.
- [ ] 2.3 `task dev:manifests dev:generate`, then `task dev:fmt dev:vet dev:lint dev:test` green, then commit `feat(api): declare the ContractsFulfilled condition and the inventory refusal reasons`.

## 3. The gate and the report (internal/controller, docs)

- [ ] 3.1 Add `internal/controller/platform_inventory.go` with `inventoryRefusal` and `setContractsFulfilled` per design.md § The two pure functions: sorted lists throughout, routing reported first when both refusals hold, the three condition states. Table tests in `platform_inventory_test.go` cover: over-subscribed only; comparable only; both (reason `OverSubscribedContracts`, message carries both); routable and discriminated (not refused); unfulfilled, all fulfilled and no contracts defined for the condition; and two orderings of one inventory producing one message. Verify: `go test ./internal/controller -run TestInventory` (or the Ginkgo focus the file's neighbours use) passes.
- [ ] 3.2 Wire the gate in `platform_controller.go` per design.md § Where the gate sits: `Contracts()` after the build with a read error as `BuildFailed` naming the field, `inventoryRefusal` before `SetGenerated` through `failReconcile` with a nil classify error, `setContractsFulfilled` after `recordEffectiveRegistry` on the fresh-build path and from `held.Platform.Contracts()` on the skip path (a read error there falls through to regeneration), and `ContractsFulfilledCondition` in `patchStatus`'s owned conditions. Verify: `go build ./... && go vet ./...` pass and the existing platform specs are unchanged in behaviour.
- [ ] 3.3 Refusal specs through the helper (the `platform_failure_test.go` pattern, hand-built `platform.ContractInventory` values, a store holding a `generatedMarker`): an over-subscribed inventory yields `Ready=False/OverSubscribedContracts` naming the contract, its defining catalog and both transformers; a comparable pair yields `Ready=False/ComparablePredicates` naming broader, narrower and the shared contract; both yield the routing reason with both findings; each leaves the store's identity, `status.packageIdentity`, `status.registry` and any prior `ContractsFulfilled` untouched, sets `observedGeneration`, emits one warning event and requeues on the stalled interval; a repeat of the same verdict emits no second event. Verify: the specs pass without a registry.
- [ ] 3.4 Live-registry spec (`buildKernelOrSkip`): the healthy platform reports `Ready=True/Generated` and a `ContractsFulfilled` condition whose reason and message match the inventory read off the store's held platform; a second reconcile of the same tuple takes the skip path and still carries the condition. Verify: passes under `task dev:test` with `CUE_REGISTRY` set, skips without it, and a first-boot refusal (helper pattern, empty store) leaves the store empty.
- [ ] 3.5 `docs/RENDERING.md`: the Platform section gains the two refusals (what each names, that the last good package keeps serving, that a Platform edit or claim change re-evaluates) and the `ContractsFulfilled` condition with its three reasons; the `PlatformNotReady` row's remedy names a refused first generation. Verify: the table renders in a Markdown preview, every claim in it matches a spec scenario of this change, and no reason string differs from `internal/status/conditions.go` (the repo has no Markdown lint task).
- [ ] 3.6 `task dev:fmt dev:vet dev:lint dev:test` green, then commit `feat(controller): refuse generation on an over-subscribed or undiscriminated platform`.
